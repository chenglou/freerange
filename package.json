{
  "name": "@chenglou/freerange",
  "version": "0.0.0",
  "private": true,
  "description": "Static @fit facts for ordinary TypeScript layout code",
  "type": "module",
  "bin": {
    "fr": "./fr.ts"
  },
  "scripts": {
    "test": "bun test --parallel=3 --parallel-delay=0 ./tests",
    "test:update": "FREERANGE_UPDATE_SNAPSHOTS=1 bun run test",
    "bench": "bun bench.ts",
    "verify:photo-gallery": "bun verify-photo-gallery-infer-snapshots.ts",
    "check": "bun run test && bun run verify:photo-gallery && tsc && oxlint --type-aware src tests bench.ts bench-core.ts fr.ts verify-photo-gallery-infer-snapshots.ts snapshot.ts --ignore-pattern '*.js' && bun knip"
  },
  "devDependencies": {
    "knip": "^6.14.2",
    "oxlint": "latest",
    "oxlint-tsgolint": "latest",
    "typescript": "^6.0.2"
  }
}
