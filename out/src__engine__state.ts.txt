src/engine/state.ts

freshSlotVersion
  ensures: return is a finite number
  assumes: slotVersionCounter is finite and not NaN

hasValidIndexPair
  unsupported: function call pairs.some (array methods are outside the subset; rewrite as a for-of loop over the same array) at src/engine/state.ts:62:10

addValidIndexPair
  unsupported: function call pairs.push (array methods are outside the subset; rewrite as a for-of loop over the same array) at src/engine/state.ts:66:48

dropModuleRootedPairs
  unsupported: expression (ArrowFunction) at src/engine/state.ts:77:20

emptySharedState
  unsupported: function call modules.push (array methods are outside the subset; rewrite as a for-of loop over the same array) at src/engine/state.ts:85:53

cloneSharedState
  unsupported: function call state.modules.slice (array methods are outside the subset; rewrite as a for-of loop over the same array) at src/engine/state.ts:91:20

cloneState
  unsupported: function call (…).slice (array methods are outside the subset; rewrite as a for-of loop over the same array) at src/engine/state.ts:96:21

joinStates
  unsupported: binary operator = (supported: + - * / %, comparisons, and boolean && || !) at src/engine/state.ts:109:28

joinModuleSlots
  unsupported: function call joined.push (array methods are outside the subset; rewrite as a for-of loop over the same array) at src/engine/state.ts:142:5

sameState
  unsupported: expression (VoidExpression) at src/engine/state.ts:164:3

widenState
  unsupported: binary operator = (supported: + - * / %, comparisons, and boolean && || !) at src/engine/state.ts:203:7