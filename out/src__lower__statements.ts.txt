src/lower/statements.ts

lowerStatements
  assumes: context.sites[each].start is finite and not NaN
  assumes: context.sites[each].end is finite and not NaN
  assumes: context.nextValue is finite and not NaN
  assumes: context.currentBlock.loopHeader is null or a finite non-NaN number
  assumes: every context.currentBlock.parameters element is finite and not NaN
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'constant')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'constant')
  assumes: context.currentBlock.instructions[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'constant')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'nullishConstant')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'nullishConstant')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'opaqueConstant')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'opaqueConstant')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'unknownBoolean')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'unknownBoolean')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayLiteral')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayLiteral')
  assumes: every context.currentBlock.instructions[each].elements element is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayLiteral')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayLength')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayLength')
  assumes: context.currentBlock.instructions[each].array is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayLength')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayIndex')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayIndex')
  assumes: context.currentBlock.instructions[each].array is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayIndex')
  assumes: context.currentBlock.instructions[each].index is finite and not NaN (when context.currentBlock.instructions[each].kind is 'arrayIndex')
  assumes: context.currentBlock.instructions[each].asserted is a boolean (when context.currentBlock.instructions[each].kind is 'arrayIndex')
  assumes: context.currentBlock.instructions[each].provenBounds is a boolean (when context.currentBlock.instructions[each].kind is 'arrayIndex')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'tagCheck')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'tagCheck')
  assumes: context.currentBlock.instructions[each].union is finite and not NaN (when context.currentBlock.instructions[each].kind is 'tagCheck')
  assumes: context.currentBlock.instructions[each].negated is a boolean (when context.currentBlock.instructions[each].kind is 'tagCheck')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'inCheck')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'inCheck')
  assumes: context.currentBlock.instructions[each].union is finite and not NaN (when context.currentBlock.instructions[each].kind is 'inCheck')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'nullishCheck')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'nullishCheck')
  assumes: context.currentBlock.instructions[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'nullishCheck')
  assumes: context.currentBlock.instructions[each].negated is a boolean (when context.currentBlock.instructions[each].kind is 'nullishCheck')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'booleanConstant')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'booleanConstant')
  assumes: context.currentBlock.instructions[each].value is a boolean (when context.currentBlock.instructions[each].kind is 'booleanConstant')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleRead')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleRead')
  assumes: context.currentBlock.instructions[each].binding is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleRead')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleWrite')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleWrite')
  assumes: context.currentBlock.instructions[each].binding is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleWrite')
  assumes: context.currentBlock.instructions[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleWrite')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleHavoc')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleHavoc')
  assumes: context.currentBlock.instructions[each].binding is finite and not NaN (when context.currentBlock.instructions[each].kind is 'moduleHavoc')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'binary')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'binary')
  assumes: context.currentBlock.instructions[each].left is finite and not NaN (when context.currentBlock.instructions[each].kind is 'binary')
  assumes: context.currentBlock.instructions[each].right is finite and not NaN (when context.currentBlock.instructions[each].kind is 'binary')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'compare')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'compare')
  assumes: context.currentBlock.instructions[each].left is finite and not NaN (when context.currentBlock.instructions[each].kind is 'compare')
  assumes: context.currentBlock.instructions[each].right is finite and not NaN (when context.currentBlock.instructions[each].kind is 'compare')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'floor')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'floor')
  assumes: context.currentBlock.instructions[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'floor')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'platformValue')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'platformValue')
  assumes: context.currentBlock.instructions[each].lower is finite and not NaN (when context.currentBlock.instructions[each].kind is 'platformValue')
  assumes: context.currentBlock.instructions[each].upper is finite and not NaN (when context.currentBlock.instructions[each].kind is 'platformValue')
  assumes: context.currentBlock.instructions[each].integer is a boolean (when context.currentBlock.instructions[each].kind is 'platformValue')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'absolute')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'absolute')
  assumes: context.currentBlock.instructions[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'absolute')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'mathUnary')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'mathUnary')
  assumes: context.currentBlock.instructions[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'mathUnary')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'stringLength')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'stringLength')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'parsedNumber')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'parsedNumber')
  assumes: context.currentBlock.instructions[each].integer is a boolean (when context.currentBlock.instructions[each].kind is 'parsedNumber')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'numberCheck')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'numberCheck')
  assumes: context.currentBlock.instructions[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'numberCheck')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'not')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'not')
  assumes: context.currentBlock.instructions[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'not')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'minimum')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'minimum')
  assumes: every context.currentBlock.instructions[each].values element is finite and not NaN (when context.currentBlock.instructions[each].kind is 'minimum')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'maximum')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'maximum')
  assumes: every context.currentBlock.instructions[each].values element is finite and not NaN (when context.currentBlock.instructions[each].kind is 'maximum')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'call')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'call')
  assumes: context.currentBlock.instructions[each].function is finite and not NaN (when context.currentBlock.instructions[each].kind is 'call')
  assumes: every context.currentBlock.instructions[each].arguments element is finite and not NaN (when context.currentBlock.instructions[each].kind is 'call')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'object')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'object')
  assumes: context.currentBlock.instructions[each].properties[each].value is finite and not NaN (when context.currentBlock.instructions[each].kind is 'object')
  assumes: context.currentBlock.instructions[each].result is finite and not NaN (when context.currentBlock.instructions[each].kind is 'property')
  assumes: context.currentBlock.instructions[each].site is finite and not NaN (when context.currentBlock.instructions[each].kind is 'property')
  assumes: context.currentBlock.instructions[each].object is finite and not NaN (when context.currentBlock.instructions[each].kind is 'property')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.value is null or a finite non-NaN number (when context.currentBlock.terminator.kind is 'return')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.site is finite and not NaN (when context.currentBlock.terminator.kind is 'return')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.target.block is finite and not NaN (when context.currentBlock.terminator.kind is 'jump')
  assumes: context.currentBlock.terminator is null or every context.currentBlock.terminator.target.arguments element is finite and not NaN (when context.currentBlock.terminator.kind is 'jump')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.site is finite and not NaN (when context.currentBlock.terminator.kind is 'jump')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.condition is finite and not NaN (when context.currentBlock.terminator.kind is 'branch')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.whenTrue.block is finite and not NaN (when context.currentBlock.terminator.kind is 'branch')
  assumes: context.currentBlock.terminator is null or every context.currentBlock.terminator.whenTrue.arguments element is finite and not NaN (when context.currentBlock.terminator.kind is 'branch')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.whenFalse.block is finite and not NaN (when context.currentBlock.terminator.kind is 'branch')
  assumes: context.currentBlock.terminator is null or every context.currentBlock.terminator.whenFalse.arguments element is finite and not NaN (when context.currentBlock.terminator.kind is 'branch')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.site is finite and not NaN (when context.currentBlock.terminator.kind is 'branch')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.site is finite and not NaN (when context.currentBlock.terminator.kind is 'stop')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.reason.arrayMethod is undefined or a boolean (when context.currentBlock.terminator.reason.kind is 'call') (when context.currentBlock.terminator.kind is 'stop')
  assumes: context.currentBlock.terminator is null or context.currentBlock.terminator.site is finite and not NaN (when context.currentBlock.terminator.kind is 'thrown')
  assumes: context.blocks[each].loopHeader is null or a finite non-NaN number
  assumes: every context.blocks[each].parameters element is finite and not NaN
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'constant')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'constant')
  assumes: context.blocks[each].instructions[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'constant')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'nullishConstant')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'nullishConstant')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'opaqueConstant')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'opaqueConstant')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'unknownBoolean')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'unknownBoolean')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayLiteral')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayLiteral')
  assumes: every context.blocks[each].instructions[each].elements element is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayLiteral')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayLength')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayLength')
  assumes: context.blocks[each].instructions[each].array is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayLength')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayIndex')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayIndex')
  assumes: context.blocks[each].instructions[each].array is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayIndex')
  assumes: context.blocks[each].instructions[each].index is finite and not NaN (when context.blocks[each].instructions[each].kind is 'arrayIndex')
  assumes: context.blocks[each].instructions[each].asserted is a boolean (when context.blocks[each].instructions[each].kind is 'arrayIndex')
  assumes: context.blocks[each].instructions[each].provenBounds is a boolean (when context.blocks[each].instructions[each].kind is 'arrayIndex')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'tagCheck')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'tagCheck')
  assumes: context.blocks[each].instructions[each].union is finite and not NaN (when context.blocks[each].instructions[each].kind is 'tagCheck')
  assumes: context.blocks[each].instructions[each].negated is a boolean (when context.blocks[each].instructions[each].kind is 'tagCheck')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'inCheck')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'inCheck')
  assumes: context.blocks[each].instructions[each].union is finite and not NaN (when context.blocks[each].instructions[each].kind is 'inCheck')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'nullishCheck')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'nullishCheck')
  assumes: context.blocks[each].instructions[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'nullishCheck')
  assumes: context.blocks[each].instructions[each].negated is a boolean (when context.blocks[each].instructions[each].kind is 'nullishCheck')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'booleanConstant')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'booleanConstant')
  assumes: context.blocks[each].instructions[each].value is a boolean (when context.blocks[each].instructions[each].kind is 'booleanConstant')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleRead')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleRead')
  assumes: context.blocks[each].instructions[each].binding is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleRead')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleWrite')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleWrite')
  assumes: context.blocks[each].instructions[each].binding is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleWrite')
  assumes: context.blocks[each].instructions[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleWrite')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleHavoc')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleHavoc')
  assumes: context.blocks[each].instructions[each].binding is finite and not NaN (when context.blocks[each].instructions[each].kind is 'moduleHavoc')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'binary')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'binary')
  assumes: context.blocks[each].instructions[each].left is finite and not NaN (when context.blocks[each].instructions[each].kind is 'binary')
  assumes: context.blocks[each].instructions[each].right is finite and not NaN (when context.blocks[each].instructions[each].kind is 'binary')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'compare')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'compare')
  assumes: context.blocks[each].instructions[each].left is finite and not NaN (when context.blocks[each].instructions[each].kind is 'compare')
  assumes: context.blocks[each].instructions[each].right is finite and not NaN (when context.blocks[each].instructions[each].kind is 'compare')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'floor')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'floor')
  assumes: context.blocks[each].instructions[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'floor')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'platformValue')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'platformValue')
  assumes: context.blocks[each].instructions[each].lower is finite and not NaN (when context.blocks[each].instructions[each].kind is 'platformValue')
  assumes: context.blocks[each].instructions[each].upper is finite and not NaN (when context.blocks[each].instructions[each].kind is 'platformValue')
  assumes: context.blocks[each].instructions[each].integer is a boolean (when context.blocks[each].instructions[each].kind is 'platformValue')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'absolute')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'absolute')
  assumes: context.blocks[each].instructions[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'absolute')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'mathUnary')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'mathUnary')
  assumes: context.blocks[each].instructions[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'mathUnary')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'stringLength')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'stringLength')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'parsedNumber')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'parsedNumber')
  assumes: context.blocks[each].instructions[each].integer is a boolean (when context.blocks[each].instructions[each].kind is 'parsedNumber')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'numberCheck')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'numberCheck')
  assumes: context.blocks[each].instructions[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'numberCheck')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'not')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'not')
  assumes: context.blocks[each].instructions[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'not')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'minimum')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'minimum')
  assumes: every context.blocks[each].instructions[each].values element is finite and not NaN (when context.blocks[each].instructions[each].kind is 'minimum')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'maximum')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'maximum')
  assumes: every context.blocks[each].instructions[each].values element is finite and not NaN (when context.blocks[each].instructions[each].kind is 'maximum')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'call')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'call')
  assumes: context.blocks[each].instructions[each].function is finite and not NaN (when context.blocks[each].instructions[each].kind is 'call')
  assumes: every context.blocks[each].instructions[each].arguments element is finite and not NaN (when context.blocks[each].instructions[each].kind is 'call')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'object')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'object')
  assumes: context.blocks[each].instructions[each].properties[each].value is finite and not NaN (when context.blocks[each].instructions[each].kind is 'object')
  assumes: context.blocks[each].instructions[each].result is finite and not NaN (when context.blocks[each].instructions[each].kind is 'property')
  assumes: context.blocks[each].instructions[each].site is finite and not NaN (when context.blocks[each].instructions[each].kind is 'property')
  assumes: context.blocks[each].instructions[each].object is finite and not NaN (when context.blocks[each].instructions[each].kind is 'property')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.value is null or a finite non-NaN number (when context.blocks[each].terminator.kind is 'return')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.site is finite and not NaN (when context.blocks[each].terminator.kind is 'return')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.target.block is finite and not NaN (when context.blocks[each].terminator.kind is 'jump')
  assumes: context.blocks[each].terminator is null or every context.blocks[each].terminator.target.arguments element is finite and not NaN (when context.blocks[each].terminator.kind is 'jump')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.site is finite and not NaN (when context.blocks[each].terminator.kind is 'jump')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.condition is finite and not NaN (when context.blocks[each].terminator.kind is 'branch')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.whenTrue.block is finite and not NaN (when context.blocks[each].terminator.kind is 'branch')
  assumes: context.blocks[each].terminator is null or every context.blocks[each].terminator.whenTrue.arguments element is finite and not NaN (when context.blocks[each].terminator.kind is 'branch')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.whenFalse.block is finite and not NaN (when context.blocks[each].terminator.kind is 'branch')
  assumes: context.blocks[each].terminator is null or every context.blocks[each].terminator.whenFalse.arguments element is finite and not NaN (when context.blocks[each].terminator.kind is 'branch')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.site is finite and not NaN (when context.blocks[each].terminator.kind is 'branch')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.site is finite and not NaN (when context.blocks[each].terminator.kind is 'stop')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.reason.arrayMethod is undefined or a boolean (when context.blocks[each].terminator.reason.kind is 'call') (when context.blocks[each].terminator.kind is 'stop')
  assumes: context.blocks[each].terminator is null or context.blocks[each].terminator.site is finite and not NaN (when context.blocks[each].terminator.kind is 'thrown')
  assumes: context.parameters[each].value is finite and not NaN
  stopped: calls lowerStatement, which hit unsupported code (call at src/lower/statements.ts:22:5)

lowerStatement
  unsupported: function call ts.isVariableStatement at src/lower/statements.ts:27:7

lowerIfStatement
  unsupported: a write into an object (values are immutable; rebind a variable to a fresh object instead) at src/lower/statements.ts:93:5

lowerSwitchStatement
  unsupported: a write into an object (values are immutable; rebind a variable to a fresh object instead) at src/lower/statements.ts:208:7

lowerForOfStatement
  unsupported: a write into an object (values are immutable; rebind a variable to a fresh object instead) at src/lower/statements.ts:275:3

lowerForStatement
  unsupported: a write into an object (values are immutable; rebind a variable to a fresh object instead) at src/lower/statements.ts:340:3

lowerBranch
  unsupported: a write into an object (values are immutable; rebind a variable to a fresh object instead) at src/lower/statements.ts:372:3

lowerVariableDeclarationList
  unsupported: value of type NodeArray<VariableDeclaration> at src/lower/statements.ts:379:29

assignedSymbols
  unsupported: function parameter with type TypeChecker at src/lower/statements.ts:439:44